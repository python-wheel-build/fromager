# Pinned packages for benchmark isolation
# These packages are downloaded to the local PyPI server for network-isolated benchmarks.
#
# Packages chosen for:
# - Common use in resolution benchmarks
# - No native dependencies (pure Python)
# - Small download size

packaging==23.2
requests==2.31.0
urllib3==2.1.0
charset-normalizer==3.3.2
idna==3.6
certifi==2024.2.2
pydantic==2.5.3
pydantic-core==2.14.6
annotated-types==0.6.0
typing-extensions==4.9.0
